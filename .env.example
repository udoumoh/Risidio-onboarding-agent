# Slack Configuration (Required)
# Get your bot token from https://api.slack.com/apps/YOUR_APP_ID/oauth-tokens
# The token should start with "xoxb-" (bot token, not xoxp- which is a user token)
SLACK_BOT_TOKEN=xoxb-your-token-here

# Slack app signing secret for verifying requests
# Found in your Slack app settings under "Basic Information" > "App Credentials"
SLACK_SIGNING_SECRET=your-signing-secret-here

# LLM API Keys (At least one is required)
# OpenAI API key for GPT-based responses
# Get from https://platform.openai.com/api-keys
# Optional if ANTHROPIC_API_KEY is set, otherwise required
OPENAI_API_KEY=sk-proj-your-openai-key-here

# Anthropic API key for Claude-based responses
# Get from https://console.anthropic.com/
# Optional if OPENAI_API_KEY is set, otherwise required
ANTHROPIC_API_KEY=your-anthropic-key-here

# Server Configuration (Optional)
# Port the bot server runs on (defaults to 3000 if not set)
PORT=3000

# Node Environment (Optional, defaults to development)
# Set to "production" when deploying to production
NODE_ENV=development
